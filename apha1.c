#include <stdio.h>
int main()
{
  char ip;
printf("\n enter the alphabet");
  scanf("%c",&ip);
  if(ip==65&&90||97&&122)
    printf("\n the given thing is alphabet");
 else
    printf("\n the given thing is not alphabet");
	return 0;
}
